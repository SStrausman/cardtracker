<% @search_results.each do |card| %>
  <%= card.name %>
  <%= card.card_set_name %>
  <% url = "http://magictcgprices.appspot.com/api/cfb/price.json?cardname=#{card.name.gsub(/\s/,'%20')}&setname=#{card.card_set_id}" %>
  <%= HTTParty.get(url).pop %>
<% end %>